1. bulk video convert side pane does NOT look good. There's weird spacing issues. The title "Bulk Convert" is strangely too close to the left, mislenged with the left margin of the rest of the UI elements. There's a "0" on the right side of "Resolution". What is this? What does it do? Just remove it. When I enter resolution, it offers to let me clear it, that is good. The "10 of 10 file selected" message has very little value since we already show Convert 10 files in action button. It visually looks like a broken input field as opposed to info. Just remove it. Take a screenshot of thie panel and see for yourself.
2. Open a video in video mode, play or seek to a certain point. Click on Gallery tab and then back to Video tab. Now the timeline position reset to 1 second every time. It should preserve where the timeline position was. Switching tabs should not reset it. 
3. In gallery grid view, when I click and focus on a caption text to edit, it turns the lower half overlay to a grey-ish color. I don't like this color, just change it to a transparent black to align with the unfocused state. Perhaps a bit less opacity than the unfocused state overlay color.
4. We previously renamed the two tabs from "Clip" and "Gallery" to "Video" and "Gallery", but there are still code references that refers to the "Video" tab as "clip". Let's audit the code and fix that in code. Be cautious not to confuse with the "Clip" action button, which was why we decided to rename it in the first place.
5. In gallery view, rename "Bulk Convert" button to just "Convert" and it should use a primary color. This is an important part of the workflow.
6. In gallery view, similar to "Convert" also offer a "Delete" action button that's disabled when no file is selected, but if there are files selected, allow bulk delete. Make sure we have a confirmation dialog.
7. When a video is expanded in gallery tab. the video plays with a `x` close button ON TOP outside of the video. This is different visual experience as the video tab, where the `x` close button in INSIDE the video on its right top corner without creating a spacing gap on the top of the video. We should align the look and feel experience and put the x inside the video on right top corner without a gap on top of the video.
8. in the Conver side pane, the CODEC drop down has WHITE background on WHITE text, making it unredable. Take a screenshot and see for yourself.
9. in Video tab, instead of "Length:" can we replace it with an icon like ruler-dimension-line? Also the 's' that indicates seconds is too small. It's hard to see. Just use the same font size as the rest of the text there.